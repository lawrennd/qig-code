numpy>=1.21.0
scipy>=1.7.0
matplotlib>=3.4.0
pytest>=7.0.0
nbstripout>=0.6.0
nbformat>=5.0.0  # Optional: for notebook smoke tests (pytest -m integration)
nbconvert>=7.0.0  # Optional: for full notebook testing (pytest -m "integration and slow")

